import { Meta, Story, Canvas, Primary, ArgsTable } from '@storybook/addon-docs';
import { StoryCategories } from '../../categories';
import IconDocs from '../../../packages/core/src/components/atoms/icon/readme.md';
import { categorized, icons } from '../../../packages/core/src/utils/icons';

<Meta title={`${StoryCategories.Atoms}/Rebranding/Icon`} />

<IconDocs />

# Examples

## Variants

### Categorized

<Canvas>
	<div class='flex flex-col gap-12'>
		{Object.keys(categorized).map(category => (
			<div class='flex flex-col gap-6'>
				<h2>{category}</h2>
				<div class='flex w-full flex-wrap gap-4'>
					{Object.keys(categorized[category]).map(variant => (
						<p-tooltip content={variant}>
							<p-icon
								key={variant}
								variant={variant}
								class='text-2xl text-black-teal hover:scale-125 hover:text-supportive-lilac'
								slot='trigger'
							></p-icon>
						</p-tooltip>
					))}
				</div>
			</div>
		))}
	</div>
</Canvas>

### List

<Canvas>
	<table class='w-full table-fixed'>
		<tr>
			<th class='py-2 text-start'>Icon</th>
			<th class='py-2 text-start'>Variant</th>
		</tr>
		{Object.keys(icons).map(icon => (
			<tr class='group border-b border-b-mystic-dark'>
				<td class='py-2'>
					<p-icon
						key={icon}
						variant={icon}
						class='text-2xl text-black-teal group-hover:text-supportive-lilac'
					></p-icon>
				</td>
				<td class='py-2 group-hover:font-semibold group-hover:text-supportive-lilac'>
					{icon}
				</td>
			</tr>
		))}
	</table>
</Canvas>

### Flipping

#### Horizontal

<Canvas>
	<div class='flex'>
		<p-icon
			variant='arrow'
			flip='horizontal'
		/>
	</div>
</Canvas>

#### Vertical

<Canvas>
	<div class='flex'>
		<p-icon
			variant='user'
			flip='vertical'
		/>
	</div>
</Canvas>

### Rotation

#### 45 degrees

<Canvas>
	<div class='flex'>
		<p-icon
			variant='arrow'
			rotate={45}
		/>
	</div>
</Canvas>

#### -25 degrees

<Canvas>
	<div class='flex'>
		<p-icon
			variant='arrow'
			rotate={-25}
		/>
	</div>
</Canvas>
