import { Meta, Story, Canvas, Primary, ArgsTable } from '@storybook/addon-docs';
import { StoryCategories } from '../../categories';
import IllustrationDocs from '../../../packages/core/src/components/atoms/illustration/readme.md';
import illustrations from '../../../packages/core/src/utils/illustrations';

<Meta title={`${StoryCategories.Atoms}/Illustration`} />

<IllustrationDocs />

# Illustrations

<Canvas>
    <div class="grid grid-cols-1 gap-10 text-5xl">
        {Object.keys(illustrations).map((variant) => (
            <p-illustration key={variant} variant={variant}></p-illustration>
        ))}
    </div>
</Canvas>
